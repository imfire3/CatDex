{
  "name": "catdex",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "mobile",
    "packages/*"
  ],
  "scripts": {
    "backend": "npm run dev --workspace=backend",
    "mobile": "npm run start --workspace=mobile",
    "install:all": "npm install"
  }
}
